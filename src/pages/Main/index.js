import React from 'react';

import { Title } from './styles';

export default function Main() {
  return (
    <Title>
      React App with following configurations:
      <small>
        ESLint, Prettier, EditorConfig, Roteamento React & Styled Components
      </small>
    </Title>
  );
}
